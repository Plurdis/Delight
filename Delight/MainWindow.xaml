
<Window
    x:Class="Delight.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:Delight.Common"
    xmlns:ct="clr-namespace:Delight.Component.Controls;assembly=Delight.Component"
    xmlns:cvt="clr-namespace:Delight.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:effects="clr-namespace:Delight.Component.Effects;assembly=Delight.Component"
    xmlns:local="clr-namespace:Delight"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="clr-namespace:Delight.Pages"
    xmlns:stageComponent="clr-namespace:Delight.Core.Stage.Components;assembly=Delight.Core"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    Title="MainWindow"
    Width="1300"
    Height="800"
    mc:Ignorable="d">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Menu x:Name="topMenu" ItemsSource="{Binding Menus}" />

        <ToolBarTray Grid.Row="1">
            <ToolBar Band="1" BandIndex="1">
                <ToolBar.Resources>
                    <DataTemplate DataType="{x:Type RoutedCommand}">
                        <TextBlock>
                            <Run Text="String: " />
                            <Run Text="{Binding Path=.}" />
                        </TextBlock>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type sys:Int32}">
                        <TextBlock>
                            <Run Text="Int32: " />
                            <Run Text="{Binding Path=.}" />
                        </TextBlock>
                    </DataTemplate>
                </ToolBar.Resources>
            </ToolBar>
        </ToolBarTray>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="350" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="360" />
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="200" />
                </Grid.RowDefinitions>
                <Grid>
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="116" />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <Grid Background="{StaticResource Delight.BackgroundColor}">
                            <TextBlock
                                Margin="10"
                                FontFamily="/Delight;component/Resources/Fonts/#Helvetica"
                                TextWrapping="Wrap">
                                <Run
                                    FontSize="40"
                                    FontWeight="Bold"
                                    Foreground="White"
                                    Text="아이템" /><LineBreak />
                                <TextBlock FontSize="5" />
                                <LineBreak />
                                <Run Foreground="White" Text="원하는 요소들을 끌어다가 타임라인에 배치하세요" />
                            </TextBlock>
                        </Grid>
                        <Grid Grid.Row="1" Background="#FFEBEBEB">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="47" />
                                <ColumnDefinition Width="2" />
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <Grid Grid.ColumnSpan="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>
                                <WrapPanel HorizontalAlignment="Left">
                                    <WrapPanel.Resources>
                                        <Style TargetType="{x:Type RadioButton}">
                                            <Setter Property="Width" Value="25" />
                                            <Setter Property="Height" Value="25" />
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type RadioButton}">
                                                        <Grid>
                                                            <Border
                                                                x:Name="selectBorder"
                                                                Background="Transparent"
                                                                BorderBrush="#FF003AFF"
                                                                BorderThickness="0" />
                                                            <ContentPresenter
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Content="{TemplateBinding Content}" />
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsChecked" Value="True">
                                                                <Setter TargetName="selectBorder" Property="BorderThickness" Value="1" />
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </WrapPanel.Resources>
                                    <RadioButton x:Name="rbList">
                                        <Image Margin="5" Source="Resources/view_block.png" />
                                    </RadioButton>
                                    <RadioButton x:Name="rbBox">
                                        <Image Margin="5" Source="Resources/view_list_sel.png" />
                                    </RadioButton>
                                </WrapPanel>

                                <Grid Grid.Column="1" Background="White">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Margin" Value="5,0" />
                                            <!--  <Setter Property="FontFamily" Value="/Delight;component/Resources/Fonts/#Helvetica"/>  -->
                                            <Setter Property="Foreground" Value="#FF505050" />
                                            <Setter Property="FontWeight" Value="Bold" />
                                            <Setter Property="VerticalAlignment" Value="Center" />
                                        </Style>
                                    </Grid.Resources>
                                    <TextBlock Grid.Column="1" Text="새 폴더" />
                                    <TextBlock Grid.Column="2" Text="편집" />
                                </Grid>


                            </Grid>

                            <ListBox
                                x:Name="ItemHeader"
                                Grid.Row="1"
                                Background="{x:Null}"
                                BorderBrush="{x:Null}"
                                FontFamily="/Delight;component/Resources/Fonts/#Helvetica"
                                SelectedIndex="0">
                                <ListBox.ItemContainerStyle>
                                    <Style TargetType="ListBoxItem">
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                                        <Setter Property="Foreground" Value="Black" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ListBoxItem">
                                                    <Grid Background="Transparent">
                                                        <ContentPresenter
                                                            Margin="0,5"
                                                            HorizontalAlignment="Right"
                                                            Content="{TemplateBinding Content}" />
                                                    </Grid>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsSelected" Value="True">
                                                            <Setter Property="FontSize" Value="14" />
                                                            <Setter Property="FontWeight" Value="Bold" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ListBox.ItemContainerStyle>
                                <ListBoxItem Content="전체" Tag="127" />
                                <ListBoxItem Content="사진" Tag="1" />
                                <ListBoxItem Content="영상" Tag="2" />
                                <ListBoxItem Content="조명" Tag="64" />
                                <ListBoxItem Content="소리" Tag="8" />
                            </ListBox>

                            <Rectangle
                                Grid.Row="1"
                                Grid.Column="1"
                                Margin="0,10"
                                Fill="#FFBEBEBE" />

                            <Grid Grid.Row="1" Grid.Column="2">
                                <Grid Grid.Row="1">
                                    <ListBox
                                        x:Name="lbMediaItem"
                                        Background="#FFEBEBEB"
                                        BorderBrush="{x:Null}"
                                        ItemsSource="{Binding MediaItemsView}"
                                        ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                                        <ListBox.Resources>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />
                                        </ListBox.Resources>
                                        <ListBox.ItemContainerStyle>
                                            <Style TargetType="{x:Type ListBoxItem}">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListBoxItem">
                                                            <Grid Width="130" Height="88">
                                                                <Border
                                                                    x:Name="bgBorder"
                                                                    Grid.RowSpan="2"
                                                                    Background="Transparent"
                                                                    CornerRadius="10" />
                                                                <ContentPresenter />
                                                            </Grid>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="bgBorder" Property="Background" Value="LightGray" />
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListBox.ItemContainerStyle>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate DataType="{x:Type stageComponent:StageComponent}">
                                                <Grid Margin="5">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition />
                                                        <RowDefinition Height="Auto" />
                                                    </Grid.RowDefinitions>
                                                    <Image
                                                        Width="120"
                                                        Height="64"
                                                        Source="{Binding Thumbnail, Converter={cvt:ImageConverter}}"
                                                        Stretch="UniformToFill" />
                                                    <TextBlock
                                                        Grid.Row="1"
                                                        Margin="0,5,0,0"
                                                        Text="{Binding Identifier}"
                                                        TextAlignment="Center" />
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel IsItemsHost="True" Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                    </ListBox>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
                <Grid Grid.Row="1">

                </Grid>
            </Grid>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />

            <Grid Grid.Column="2">
                <TabControl
                    x:Name="tc"
                    Background="{x:Null}"
                    BorderBrush="{x:Null}"
                    SelectedIndex="{Binding ViewingIndex}">
                    <TabControl.ItemContainerStyle>
                        <Style TargetType="{x:Type TabItem}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </Style>
                    </TabControl.ItemContainerStyle>
                    <TabItem>
                        <Grid Background="#FFCFCFCF">
                            <ct:TimeLine x:Name="tl" />
                        </Grid>
                    </TabItem>
                    <TabItem>
                        <pages:TemplateShopPage x:Name="templateShop" />
                    </TabItem>
                </TabControl>
            </Grid>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" />

            <Grid Grid.Column="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid Background="{StaticResource Delight.BackgroundColor}">
                    <TextBlock
                        Margin="10"
                        VerticalAlignment="Center"
                        FontSize="30"
                        Foreground="White"
                        Text="00:00:00;24" />
                </Grid>
                <Grid Grid.Row="1">
                    <ct:PropertyGrid
                        x:Name="propGrid"
                        HelpVisible="True"
                        ToolbarVisible="True" />
                </Grid>
                <Grid Grid.Row="2">
                    <ListBox>
                        <ListBox.Resources>
                            <DataTemplate DataType="{x:Type sys:String}">
                                <TextBlock>
                                    <Run Text="String: " />
                                    <Run Text="{Binding Path=.}" />
                                </TextBlock>
                            </DataTemplate>

                            <DataTemplate DataType="{x:Type sys:Int32}">
                                <TextBlock>
                                    <Run Text="Int32: " />
                                    <Run Text="{Binding Path=.}" />
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.Resources>
                        <sys:Int32>123</sys:Int32>
                        <sys:String>asdf</sys:String>
                    </ListBox>

                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>
