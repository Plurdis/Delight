<Window x:Class="Delight.Windows.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Delight.Windows"
        mc:Ignorable="d"
        Title="LoadingWindow" Height="100" Width="250" WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ResizeMode="NoResize">
    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Margin="0,5">
                파일을 저장하고 있습니다. (<Run x:Name="currState" Text="{Binding ElementName=pg, Path=Value}"/> / <Run Text="{Binding ElementName=pg, Path=Maximum}"/>)
            </TextBlock>
            <ProgressBar x:Name="pg" Grid.Row="1" Height="10" Width="180" Maximum="9"/>
        </Grid>
    </Grid>
</Window>
